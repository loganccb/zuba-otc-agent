# Placeholder agent - returns a test reply so we can confirm the webhook works
# Replace with full agent logic once the server is confirmed live

def handle_message(phone_number: str, message: str) -> str:
    return f"Zuba OTC desk received your message: '{message}' - agent coming soon."
